#!/bin/sh
SCRIPT_NAME=$(basename "$0")
APP_NAME=${SCRIPT_NAME%.sh}

# DIR="${0%/*}"
# DIR=/opt/ihautoclicker/bin
DIR="$APPDIR/opt/app/bin"

sudo "$DIR/java" $CDS_JVM_OPTS  -p "$DIR/../app" -m com.github.imhammer.ihautoclicker/com.github.imhammer.ihautoclicker.Launch  "$@"
